# stablegen_standalone.py — required
numpy
pillow
requests
trimesh
websocket-client

# stablegen_standalone.py — optional (depth ControlNet)
pyrender
pyopengl==3.1.5

# stablegen_standalone.py — optional (pygame viewer / camera GUI)
pygame
PyOpenGL
pyglet 
# stablegen_standalone.py — optional (better UV unwrap)
# xatlas
